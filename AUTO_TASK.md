# งานเช้าอัตโนมัติ: อัปเดตราคา Kuji (kuji-price-daily)

อ่านไฟล์นี้ + PROJECT_NOTES.md ก่อนเริ่มทุกครั้ง ทำงานเองโดยไม่ต้องถามผู้ใช้ (เป็น automated run)
เป้าหมาย: หมุนอัปเดตต้นทุน Mercari ลง site/data.json ทีละล็อต จนครบทุกคอล แล้ววนใหม่

## พารามิเตอร์
- BATCH = 100 (จำนวนคอลต่อรอบ — ตั้งสูงเพื่อให้ครบ ~181 คอลใน ~2 รอบ/2 วัน; เว้นจังหวะ Mercari กัน DataDome บล็อก ระหว่างรอบ; ต้องเปิดคอม+เบราว์เซอร์ทิ้งไว้ตอน task รัน)
- ไฟล์สถานะ: PriceUpdate/cursor.json = {"index": N}  (ตำแหน่งเริ่มของรอบถัดไป)
- data: PriceUpdate/site/data.json ; keyword map: PriceUpdate/keywords.json

## ขั้นตอนต่อ 1 รอบ
1. หาเรต JPY→THB ด้วย WebSearch (ไม่ได้ใช้ 0.206) → เก็บไว้เขียนลง data.json.rate
2. โหลด data.json (เรียงตามลำดับใน collections) + cursor.json
3. เลือกคอลที่จะดึงรอบนี้ (รวม BATCH คอล) — เฉพาะคอลที่เป็น "อิจิบังคุจิ/ฟิกเกอร์" (ข้ามการ์ดเกม Pokémon TCG / กล่องบูสเตอร์ / สินค้าเดี่ยวที่ไม่ใช่คุจิ — ปล่อย jp=null)
   **ลำดับความสำคัญ (สำคัญ — ผู้ใช้สั่งจากมือถือไม่ได้ ต้องให้รอบนี้จัดการเอง):**
   ⓪ **คอลใหม่ (createDate ภายใน 7 วันจากวันรัน) → รีเฟรช "ทุกรางวัล ทุกวัน" แม้ jp มีค่าแล้ว** (ช่วงแรกราคาแกว่ง ต้องตามทุกวัน). พอเกิน 7 วันจาก createDate ให้หลุดออกจาก tier นี้ กลับเข้ารอบปกติ (①/②)
      - แหล่งวันที่: ใช้ `createdate.json` **เท่านั้น** (id -> "YYYY-MM-DD" = วันที่ create ของสินค้าใน CSV Shopee, เติมอัตโนมัติโดย merge_shopee.py). **อย่า** fallback ไป addedstate.json เพราะ addedstate เป็นวันเดียวรวม (ตอน bulk import) จะทำให้ทุกคอลเข้า tier ⓪ พร้อมกัน. ถ้า id ไม่มีใน createdate.json = ไม่ถือว่าใหม่ (ไม่ daily-refresh)
      - ถ้า createdate.json ว่าง ({}) → tier ⓪ ว่าง ไม่มีผลข้างเคียง (รอผู้ใช้รัน merge_shopee.py ด้วย CSV Shopee ที่มีคอลัมน์วันที่สร้างก่อน)
      - ตัวอย่างเลือก: `[cid for cid,dt in createdate.items() if (today - parse(dt)).days <= 7]`
      - คอลกลุ่มนี้ "ไม่ข้าม" แม้รางวัลครบ — ดึงทับค่าเดิมด้วยราคาล่าสุด (เฉพาะค่าที่ดึงเจอจริง ไม่เขียนทับด้วย null)
   ① คอลคุจิที่ "ยังไม่อัป" (มีรางวัล jp=null) เรียง addedDate ใหม่สุดก่อน → คอลที่เพิ่ง merge เข้ามาจะได้ราคาทันทีรอบถัดไป
   ② ที่เหลือเติมด้วยการหมุน cursor ตามปกติ (index ถึง index+BATCH-1, วนกลับต้นถ้าเกิน) เพื่อรีเฟรชคอลเก่า
   ข้าม คอลที่ jp ครบทุกรางวัลแล้วในรอบนั้น **(ยกเว้น tier ⓪ ที่ต้องรีเฟรชทุกวัน)**
4. แต่ละคอล:
   a. keyword = keywords.json.map[id] ; ถ้าไม่มี → ดูชื่อสินค้า แปลงเป็นชื่อชุดญี่ปุ่น (ซีรีส์+subtitle เจาะจงชุด ไม่ใช่ชื่อซีรีส์กว้าง) → เก็บลง keywords.json.map[id]
   b. **วิธีดึง Mercari (เรียงตามความน่าเชื่อถือ):**
      - **(แนะนำที่สุด ตั้งแต่ 2026-08-31) `node mercari_fetch.cjs`** — ยิง Mercari search API (`api.mercari.jp/v2/entities:search`) พร้อม DPoP token ที่เซ็นเอง **ไม่ต้องใช้เบราว์เซอร์ ไม่ติด DataDome และรันในแซนด์บ็อกซ์/คลาวด์ของ Claude ได้** (ไม่ต้องเปิดคอมทิ้งไว้อีกต่อไป)
        - `node mercari_fetch.cjs --limit 100 --rate 0.2069` = เลือกคอลตามลำดับ ⓪①② ในข้อ 3 ให้เอง แล้วเขียน `site/data.json` + `cursor.json` + `site/notfound.json`
        - `--ids 123,456` = เจาะจงคอล, `--dry-run` = ไม่เขียนไฟล์, `--verbose` = โชว์ราคาที่เลือกรายรางวัล
        - บน Node เวอร์ชันที่ใช้ proxy ของแซนด์บ็อกซ์ ต้องตั้ง `NODE_USE_ENV_PROXY=1` นำหน้าคำสั่ง
        - ตัวกรองในสคริปต์: ต้องมีชื่อรางวัลตรง (`A賞`/`ラストワン`), ไม่มีรางวัลตัวอื่นปน, ตัด まとめ/コンプ/セット/空箱/台座/支柱/ジャンク/応募 และรายการที่ขึ้นต้นด้วย `<ชื่อ>様` (จองแล้ว) แล้วเลือกถูกสุดที่เหลือ
        - รางวัลที่ค้นแล้วไม่เจอ ถูกบันทึกลง `site/notfound.json` (คีย์ `"<id>|<pz>"`) ให้เว็บขึ้นป้าย "ไม่เจอของขาย" และรายละเอียดรอบล่าสุดอยู่ใน `fetch_log.json`
      - **(สำรอง) Claude in Chrome** — ถ้า API เปลี่ยนสัญญาแล้ว fetch พัง ค่อยใช้เบราว์เซอร์จริง:
        1. `mcp__Claude_in_Chrome__navigate` ไป `https://jp.mercari.com/search?keyword=<urlenc(คีย์เวิร์ด + 'A賞'/'B賞'/.../'ラストワン')>&status=on_sale&sort=price&order=asc`
        2. `mcp__Claude_in_Chrome__javascript_tool` ดึงรายการ: วน `li[data-testid="item-cell"]`, อ่าน aria-label ของ `.merItemThumbnail` (มีราคาเยนจริงเสมอ เช่น "...300 yen (฿65.70)") → regex `/([\d,]+)\s*yen/` ได้เยน, ชื่อจาก aria-label
           ⚠️ หน้าอาจถูกส่วนขยายแปลภาษา/แปลงสกุล (โชว์ ฿) แต่เยนจริงยังอยู่ใน aria-label — อ่านจากตรงนั้น อย่าอ่านตัวเลข ฿ ที่แปลงมา
        3. ถ้าเบราว์เซอร์ไม่ได้เชื่อม (`list_connected_browsers` ว่าง) ค่อย fallback ไป `web_fetch`/`WebSearch` (อาจได้ null)
   c. หาราคา "ซื้อได้เลยถูกสุด" ต่อรางวัล: ข้ามเซ็ตรวม/まとめ/コンプ/全種/หลายตัวอักษร, ข้ามชื่อมี "様/専用" (จองแล้ว); จับคู่ตัวอักษรรางวัล (A賞..E賞, ラストワン=LO/Last one) ให้ตรง prizes[].pz; เลือกฟิกเกอร์เดี่ยวถูกสุด — ไม่มั่นใจชุด/รางวัลให้ใส่ null ห้ามเดา
   d. เขียนเยนลง prizes[].jp (เฉพาะรางวัลที่เจอจริง; ไม่เจอ=คงค่าเดิม/null)
5. data.json.updated = วันที่วันนี้ (YYYY-MM-DD) ; บันทึก data.json + keywords.json
6. cursor.json.index = index+BATCH (mod จำนวนคอล)
7. สรุปสั้นๆ: อัปเดตกี่คอล/กี่รางวัล + รายการมาร์จิน <55% (คอล/รางวัล/มาร์จิน)
8. **เก็บประวัติ + รีเฟรชลิงก์ (สำคัญ ทำทุกรอบหลังเขียน data.json/keywords.json):**
   รัน `python3 db_update.py` (อยู่โฟลเดอร์ PriceUpdate) — สคริปต์จะ:
   - เติม collection.jpkw (คีย์เวิร์ดสะอาด) + prize.jpUrl (ลิงก์ค้น Mercari ของรางวัลนั้น) ลง site/data.json
     → ปุ่ม "ก๊อปคำขอราคา" บนเว็บจะมีคีย์เวิร์ดติดไปด้วย (เดิมว่างเลยค้นผิดชุด)
   - บันทึก snapshot ราคาวันนี้ลง history.jsonl + prices.csv + prices.sql + prices.db (ฐานข้อมูลประวัติ)
   - สร้าง site/history.json ให้เว็บวาดกราฟราคาย้อนหลัง
   idempotent: รันซ้ำวันเดิมทับแถวเดิม ไม่ซ้ำ. ใช้วันที่จาก data.json.updated อัตโนมัติ
   (ตัวเลือกเสริม: ให้ซับเอเจนต์คืน URL หน้า jp.mercari.com/item/ ที่เจอราคาถูกสุด เก็บไว้เป็น prize.jpItem ได้ ถ้าทำในอนาคต)

## ห้าม/ข้อควรระวัง
- อย่า deploy (sandbox ต่อ Netlify ไม่ได้) — แค่เขียนไฟล์ การ deploy ทำโดย Windows Task Scheduler ฝั่งผู้ใช้ (รัน deploy.bat)
- ในโฟลเดอร์ site เขียนได้แค่ data.json และ history.json (history.json สร้างโดย db_update.py อย่าแก้มือ)
- ถ้า web_fetch/WebSearch ใช้ไม่ได้ ให้ข้ามคอลนั้น คงค่าเดิม และระบุในสรุป
- จับคู่ราคาให้ระวังชุดผิด (ซีรีส์เดียวมีหลายคุจิ) — ถ้าไม่มั่นใจชุด ให้ข้ามและ mark ใน keywords.json ว่า "?ต้องเช็ก"
- **มือถือดึงราคาไม่ได้ (ข้อจำกัดเดิม — ตอนนี้เลี่ยงได้แล้ว):** ถ้ารันผ่าน `mercari_fetch.cjs` ในเซสชัน Claude Code (เว็บ/คลาวด์) จะดึงราคาได้เองโดยไม่ต้องพึ่งเบราว์เซอร์บนเครื่องผู้ใช้. ข้อความข้างล่างนี้ใช้กับวิธีเดิม (Claude in Chrome) เท่านั้น: Claude แอปมือถือ (ไม่ใช่ Cowork) เข้า Mercari ไม่ได้ — ปุ่ม "ก๊อปคำขอราคา" บนเว็บใช้ได้เฉพาะวางในแชท Cowork เดสก์ท็อป หรือปล่อยให้ schedule ดึงเอง (ข้อ 3 ลำดับความสำคัญจะเก็บคอลใหม่/ยังไม่อัปให้อัตโนมัติ) — ไม่ต้องสั่งจากมือถือ
